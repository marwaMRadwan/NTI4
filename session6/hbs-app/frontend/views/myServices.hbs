{{>header}}
{{>nav}}
<section class="container my-5">
    <div class="row">
        {{#each myServices}}
        <div class="col-3">
            <img src='{{this.img}}' class="img-fluid">
            <h2>{{this.title}}</h2>
            <p>{{this.content}}</p>
        </div>
        {{/each}}
    </div>
{{#each myarr}}
<li>{{this}}</li>
{{/each}}
</section>

{{>footer}}